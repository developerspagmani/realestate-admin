'use client';

import TenantsManager from '@/components/modules/realestate/tenants/TenantsManager';

export default function RealEstateAdminTenantsPage() {
    return <TenantsManager mode="realestate-admin" />;
}
